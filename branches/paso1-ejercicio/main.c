#include <stdio.h>

#include "motor/juego.h"

int main(int argc, char **argv)
{
	Juego juego;
	
	/* Creamos el juego */
	
	/* Comenzamos el bucle principal (game loop) */
	
	/* Destruimos el juego (si no liberamos la memoria, seremos unos guarros) */
	
	return 0;
}
